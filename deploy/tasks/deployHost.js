// Rsync to current directory
// Start pm2
// Assert `host:3000/status` returns `200` with current sha
// Add to the load balancer
const deployHost = (host) => {
  return new Promise((resolve) => {
    resolve(host)
  })
}

module.exports = deployHost
